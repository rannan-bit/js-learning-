JS
* js is high level programming language
* used for make web dynamic
* node is require to run js outside of the browser
* node.js : provide js run time environment + js libraries

*basic concept:
    - console.log(): used for display content
    - data types: 
        - primitive
        - non primitive
        - complex

        * typeof :used to check data type

        *primitive:
            * js has number and string datatypes
                -number: any number
                -string: text or words (if content is in "" or'' then it will be treated as string)

            *next datatypes: boolean
            * undefined datatypes: if a variable has not assigned a value then it shows undefined when its typeof
            * null datatypes: no value , can assign in future
            * big int datatypes:used for store very large integers thet are larger than the maximum safe integer support by the number type.
            * symbol datatypes: a  unique and immutable value, every symbol is different, even it has same description

        * we can create variable in three ways:
            -var :
                syntax: var variable_name/ var variable_name=value
            -let :
                syntax:
            -const

    * scope: scope referce to where a variable can access in your code.
        -global scope:it can access from anywhere in the program.
        -functional scop: variable declared whithin the function only accessed whithin
    *Hoisting: hoisting is a js behaviour where variable   




    13/07/2026


    -variable creation using let:
        *syntax: let variable_name / let variable_name=value 
    -let : block scope   

    Feature                 var                             let                                     const
    -----------------------------------------------------------------------------------------------------------------------------------------
    scope                   function                        block{}                                 block{}
    can be re-declared      yes                             yes                                     no
    can be re-initialized   no                              yes                                     yes
    hoisted                 yes(initialized as undefined)   yes(initialized without a value)        yes(no value)
    can be re-assignment    yes                             yes                                     no

join:

 * we can use , or + for join operation  
 * we can also use ` and ${} - tember
    - entire content inside the ``(back tick) and the variables are inside the ${}


Operators:
    -Assignment operators(=): used to assign values
    -Arithmetic operator(+): 
        - + Additon
        - - Substraction
        - * Multiplication
        - / Division
        - % Modulus
        - ** Exponentiation

    -Relational operators
        - < (less than)
        - > (greater than)
        - >= (greater than or equal)
        - <= (less than or equal)
        - != (not equal)
        - == (equal)(value only)
        - == (strict equal)(value + type)

    -Logical operators
        - && (AND)
        - !! (OR)
        - ! (NOT)
    
    -Unary Operators
        -increment (++)
        -decrement (--)

    * short term operation
        z=z+10 --> z+=10


* conditional statement
    -if
        syntax: if(condition){
            statement
        }
    -if-else
        syntax: if(condition){
            statement
        }
        else{
            statement
        }
    -else-of ladder
         syntax: if(condition){
            statement
        }
        else if(){
            statement
        }
        else if(){

        }......
    -switch                                                                                        14/07/2026



*Truthy operator:
    syntax: condition &&
    -only applicable if there is only one condition to check
    -equalent for if conditional statement

*Ternory operator:
    syntax: condition?statement:else_statement
    -equalent for if else conditional statement
    -eg:- age>=18?console.log("eligible for voting"):console.log("not eligible for voting")


*Loops
--------
    -for
    -while
        syntax: while(condition){
            statement
            update i
        }
    -do while
    -



                                                                                                                21/07/2026
    function
    ---------

    - function is a reusable block of code that perform a specific task. It runs only when it is called
    syntax: function function_name(parameteres){
        code to be executed
    }
